#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main()
{
    int n, A; cin>>n>>A;
    vector<int> a(n);
    vector<vector<int>> dp(n+10, vector<int>(10010));

    

    return 0;
}
