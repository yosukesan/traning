
#include <iostream>
#include <string>
#include <algorithm>
#include <set>
#include <cmath>
#include <vector>

typedef long long ll;

using namespace std;

int main()
{
    ll n;
    ll tmp;

    cin >> n;

    vector<ll>* adj = new vector<ll>[n];
    vector<ll>* score = new vector<ll>[n];

    for (ll i=0; i<n; ++i)
        cin >> tmp, adj[i].push_back(tmp);

    for (ll i=0; i<n; ++i)
        
    
    

    return 0;
}
