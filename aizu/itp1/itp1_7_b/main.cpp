
#include <iostream>
#include <string>
#include <algorithm>
#include <set>
#include <cmath>
#include <vector>

typedef long long ll;

using namespace std;

int main()
{
    ll n, x;

    while (cin >> n >> x)
    {
        if (n == -1 && x == -1)
            break;
    }

    ll count (0);
    for (i=1; i<n; i++)
    {
        
    }

    return 0;
}
